'use strict'

function getDependencies(tree, acc) {

    tree.dependencies.keys.forEach(function(dep) {
        var depString = dep + "@" tree.dependencies[dep].version
        if
    })
}

module.exports = getDependencies

